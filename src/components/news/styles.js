import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
   quang_cao: {
      width: '100%',
      flexDirection: 'row',
   },
   title: {
      fontSize: 18,
      color: '#4a90e2',
    
   },
});

// export default styles;
